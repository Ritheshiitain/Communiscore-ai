services:
  # Full-stack Unified Web Service
  - type: web
    name: communiscore-ai
    env: docker
    dockerfilePath: ./backend/Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8000
